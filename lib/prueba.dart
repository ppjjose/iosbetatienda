import 'package:flutter/material.dart';

class Pruebacosa extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Container(

          )
        ],
      ),
    );
  }
}